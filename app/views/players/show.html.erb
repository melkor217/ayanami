<%= react_component 'Player',
                    {total: @total,
                     url: player_path(@player, format: :json),
                     skill_limits: @skill_limits
                    } %>
